# Empty credentials intentionally disable authentication and database access.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Labelled demo content; defaults on without a backend. Set false to opt out.
DEMO_CONTENT=true
# Optional real canonical origin; no invented domain is used by default.
SITE_URL=
